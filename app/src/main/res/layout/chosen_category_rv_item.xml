<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/relativeLayout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/imageView_category"
        android:layout_width="130dp"
        android:layout_height="130dp"
        android:layout_margin="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginStart="8dp"
        android:background="@color/app_background_color"
        android:contentDescription="@string/category_icon"
        android:src="@drawable/ic_four_o_four"
        card_view:layout_constraintEnd_toEndOf="parent"
        card_view:layout_constraintStart_toStartOf="parent"
        card_view:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textView_categoryName"
        android:layout_width="130dp"
        android:layout_height="72dp"
        android:background="@color/app_background_color"
        android:fontFamily="@font/roboto_regular"
        android:gravity="center"
        android:padding="8dp"
        android:text="@string/test_category_name"
        android:textAlignment="center"
        android:textAppearance="@style/categoryName"
        card_view:layout_constraintStart_toStartOf="@+id/imageView_category"
        card_view:layout_constraintTop_toBottomOf="@+id/imageView_category" />
</android.support.constraint.ConstraintLayout>